apiVersion: v1
kind: Service
metadata:
  name: recommender
  labels:
    app: recommender
spec:
  type: ClusterIP
  selector:
    app: recommender
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommender
  labels:
    app: recommender
spec:
  replicas: 2
  selector:
    matchLabels:
      app: recommender
  template:
    metadata:
      labels:
        app: recommender
    spec:
      automountServiceAccountToken: false
      imagePullSecrets:
        - name: docker-hub-pull-secret
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
      containers:
        - name: recommender
          image: docker.io/anikolaevdo/recommender:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: ADDR
              value: ":8080"
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: recommender-env
                  key: REDIS_URL
            - name: USERS_SERVICE_URL
              valueFrom:
                secretKeyRef:
                  name: recommender-env
                  key: USERS_SERVICE_URL
            - name: ANALYTICS_SERVICE_URL
              valueFrom:
                secretKeyRef:
                  name: recommender-env
                  key: ANALYTICS_SERVICE_URL
            - name: LLM_ENDPOINT
              valueFrom:
                secretKeyRef:
                  name: recommender-env
                  key: LLM_ENDPOINT
            - name: OPENROUTER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: recommender-env
                  key: OPENROUTER_API_KEY
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 6
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
              ephemeral-storage: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
              ephemeral-storage: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
